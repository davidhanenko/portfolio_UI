import styled from 'styled-components';

const Stack = styled.section`
  padding-top: 5rem;
  color: var(--white);

  font-size: 1.5rem;

  display: flex;
  justify-content: space-between;
`;

export { Stack };
