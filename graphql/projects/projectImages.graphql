query ProjectsImages($title: String!) {
  projects(
    filters: { title: { eq: $title } }
  ) {
    data {
      id
      attributes {
        title
        images {
          data {
            id
            attributes {
              url
            }
          }
        }
      }
    }
  }
}
