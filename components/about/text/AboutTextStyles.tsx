import styled from 'styled-components';

const AboutTextStyles = styled.section`
  color: var(--white);
  h2 {
    font-size: 3rem;
    color: var(--white);
  }
  p {
    font-size: 1.5rem;
  }
`;

export { AboutTextStyles };
