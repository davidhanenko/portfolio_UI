import styled from 'styled-components';

const FooterStyles = styled.footer`
  display: flex;
  justify-content: end;

  padding: 0.5rem 2rem 1rem 0;

  p {
    color: var(--white);
    font-size: 1.4rem;
  }
`;

export { FooterStyles };
